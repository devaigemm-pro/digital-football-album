// Capa de API: contratos de endpoints, middleware del gateway y enrutado.

// API Gateway (borde): autenticación, límite de tasa, enrutado y terminación
// TLS (Task 4.1 — Requirements: 1.2, 20.1).
export * from './gateway/index.js';
